# AI Foundry Backend Example

This example demonstrates how to deploy Azure API Management with an Azure AI Foundry backend. It provisions an AI Foundry hub with an AI Services account, creates an APIM backend pointing to the AI service endpoint, and configures managed identity authentication via `set-backend-service` and `authentication-managed-identity` policies.
